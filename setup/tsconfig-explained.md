# ⚙️ Understanding `tsconfig.json` (Step 3)

Once you’ve installed TypeScript, it’s time to configure it. That’s where `tsconfig.json` comes in — your TypeScript project’s brain 🧠.

---

## 🤔 What Is `tsconfig.json`?

It’s a file that tells the TypeScript compiler:

- What files to compile
- How strict to be
- Where to put the output

Think of it like a recipe for how your TypeScript should be cooked.

---

## 🪄 Create One Automatically

In your terminal, run:

```bash
tsc --init
```
