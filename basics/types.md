# 🧠 Understanding Types in TypeScript

Types are the heart of TypeScript. They tell your code what kind of data to expect — and help you catch mistakes before they happen.

---

## 🔤 Basic Types

Here are the most common types you’ll use:

```ts
let username: string = "Desley";
let age: number = 25;
let isLoggedIn: boolean = true;
```
